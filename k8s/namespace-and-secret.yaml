apiVersion: v1
kind: Namespace
metadata:
  name: exam-platform
---
# NOTE: This is a placeholder. In real deployment, create this secret via
# `kubectl create secret generic exam-db-secret --from-literal=host=<RDS_ENDPOINT> -n exam-platform`
# Do NOT commit actual secrets to GitHub.
apiVersion: v1
kind: Secret
metadata:
  name: exam-db-secret
  namespace: exam-platform
type: Opaque
stringData:
  host: "REPLACE_WITH_RDS_ENDPOINT"
